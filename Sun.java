/**
 * This class represents a Sun.
 */
public class Sun extends Point {
    //maybe add stay method <= drawnObject()
    public Sun(double xCord, double yCord, double velocity, double size, String color, SolarSystem s) {
        super(xCord, yCord, velocity, size, color, s);
    }

    @Override
    void move() {

    }
}
